
---

# 射频微波版图结构化数据规范 (v3.3)

## 一、 核心架构思想 (Architectural Philosophy)

本数据格式专为现代射频微波 EDA 工具设计，摒弃了传统网表（如 IPC-D-356 或纯连线表）的扁平化缺陷，采用以下三大核心架构思想：

1. **逻辑拓扑与物理实现的彻底解耦 (Separation of Logic and Physics)**
* **逻辑层**：由 `.tel` 网表驱动，保证所有的管脚连接关系（`pin_nets`）100% 完整，确保 LVS（版图与原理图一致性）检查永不出错。
* **物理层**：由 `edges`、`footprints` 和 `global_constraints` 驱动，决定微带线的形状、板框大小和隔离区。


2. **意图驱动与动态吸附 (Intent-Driven & Parametric Adhesion)**
* 彻底抛弃了全盘绝对坐标（X/Y）的死板布局。对于周边匹配网络（RLC 器件），引入基于局部坐标系的 `parametric_uv`（UV 吸附）模型。器件不再是“放在板子上”，而是“挂在微带线上”，将具体的推挤决策权下放给后端的自动布线算法。


3. **分层分级的大一统字典 (Unified Hierarchical Dictionary)**
* 文件聚合了项目元数据、全局规则、库资产、节点实体和连线拓扑，任何单一模块的读取都不会产生上下文缺失。



---

## 二、 全局数据字典层级 (Data Schema Overview)

整个 YAML 文件由 **7 个一级节点（Root Keys）** 构成，引擎必须按以下逻辑顺序读取：

```yaml
---
metadata:            # 1. 档案头信息（知道这是什么项目、什么时间生成的）
global_constraints:  # 2. 全局物理约束（画出板框和禁布区，加载默认线宽）
footprints:          # 3. 封装资产库（将所有用到的焊盘尺寸存入内存缓存）
components:          # 4. 元器件实例（将器件放置在板框内或挂载到网络上）
nodes:               # 5. 物理骨架的通用节点（用于多段微带线的拼接）
terminals:           # 6. 物理骨架的外部端口
edges:               # 7. 走线与拓扑（连接器件管脚，渲染微带线骨架）

```

---

## 三、 详细字段说明与解析

### 1. `metadata` (元数据层)

**用途**：用于版本控制、项目管理和时间追溯。

```yaml
metadata:
  project_name: PA_Module_V1
  version: 1.0.0
  generator: EDA Auto-Layout Parser v3.3
  timestamp: 2026-05-12 12:00:00

```

### 2. `global_constraints` (全局边界与约束)

**用途**：划定 P&R 算法的物理活动范围（画布大小），并提供电气默认值。这是布线器读取的**第一份重要数据**。

```yaml
global_constraints:
  routing:
    default_trace_width: 0.254  # 常规逻辑线默认线宽 (mm)
    rf_trace_width: 0.5         # 默认射频走线线宽
  stackup:
    layer_count: 4              # 层叠数量
    material: RO4350B           # 板材介电参数预留
  board_outline:                # 🌟 物理边界：算法推挤的绝对极限
    type: bounding_box
    origin: { x: 0.0, y: 0.0 }
    width: 40.0
    height: 185.0
  keepout_zones:                # 🌟 禁布区：打孔、螺丝定位孔，禁止任何走线穿过
  - id: ko_screw_hole_1
    type: circle
    center: { x: 10.0, y: 10.0 }
    radius: 3.5

```

### 3. `footprints` (封装资产库)

**用途**：局部极坐标系下的物理外形定义。将封装定义集中化，避免在 `components` 中产生海量冗余数据。

* **规则**：所有 `local_x` 和 `local_y` 必须以器件的**几何中心 (0,0)** 为基准。
* **规则**：管脚名（如 `PIN_1`）必须加引号，且内部严格排序。

```yaml
footprints:
  "SC0603-TE":
    description: "3-Pin RF Transistor Package"
    dimensions:
      width: 1.00
      length: 1.85
    pins:
      PIN_1:
        local_x: -0.70
        local_y: 0
        local_orientation: 180  # 默认出线方向（角度）
        pad_geometry:
          shape: "rect"
          width: 0.91
          length: 0.97

```

### 4. `components` (元器件拓扑实例)

**用途**：承载网表的逻辑连接关系（`pin_nets`）以及物理放置意图（`placement`）。
这是本架构中最精妙的部分，根据 `placement.type` 区分两种布局形态：

#### 形态 A：绝对坐标锚定 (Fixed/Absolute Placement)

适用于主干芯片、接插件等预先在 Floorplan 阶段定死的器件。

```yaml
  U731032:
    footprint_ref: "RF4-SUM-HC12"
    placement:
      x: 29.6           # 全局绝对坐标
      y: 78.5
      rotation: 0
      is_floating: false # 锁死标志：P&R 算法绝对不能推挤它
    pin_nets:
      PIN_1: "$36N850545"

```

#### 形态 B：UV 参数化吸附 (Parametric UV Adhesion)

适用于周边射频匹配网络（如旁路电容、串联电感）。它们没有绝对坐标，而是“寄生”在逻辑网络或微带线上。

```yaml
  C731312:
    footprint_ref: "SC0402-PA"
    placement:
      type: parametric_uv           # 开启 UV 拓扑吸附
      anchor_pin: PIN_2             # 用该器件的 PIN_2 作为接触点
      reference_net: "$36N998423"   # 🌟 挂载目标：它要依附在这个网络上
      origin:
        offset_u: null              # 沿线长度 (交由算法推算)
        offset_v: auto              # 横向偏移 (交由算法决定左右位置)
    pin_nets:
      PIN_2: "$36N998423"

```

### 5. `edges`, `nodes`, `terminals` (走线与物理拓扑)

**用途**：定义网络的物理形状和逻辑连通性。这是算法构建图论（Graph）模型的基础。根据路由级别，分为三大种类：

**种类 1：纯逻辑骨架 (`logical_net`)**
当某个网络（如 `$36N998423`）的物理形状是由外部预定义的微带线组成时，主表里只保留它的“逻辑户口本”。

```yaml
  logical_net_$36N998423:
    type: logical_net
    net: "$36N998423"
    routing_class: externally_constrained # 标记：算法不要瞎走线，去读物理骨架
    connections: ["C731312.PIN_2", "U731032.PIN_4"] # LVS 校验基准

```

**种类 2：预定义的物理微带线 (`microstrip`) 🌟 [核心特征]**
外部导入的明确射频微带线几何骨架，用于约束高频射频信号的物理形态。它是“意图驱动”的最高体现，支持后端 EM（电磁仿真）和精确的阻抗控制。

```yaml
  U731031_pin1_seg2:
    type: microstrip
    net: "$36N996675"                 # 逻辑锚点：映射至网表
    routing_class: rf_constrained   # 算法路权：绝对优先，其他普通走线需避让
    connections:
    - U731031_pin1_seg1_universal_node # 拓扑起点：连接至前一段微带线的虚拟节点
    - R731130.PIN_1                    # 拓扑终点：精准连接至器件管脚
    constraint:                     # 物理约束：保障射频信号完整性 (SI)
      width: 0.94                     # 强制线宽：用于实现特定的特征阻抗 (如 50Ω)
      target_length: 1.21             # 相位约束：强制走线物理长度，保证微波相位准确
    geometry:                       # 几何形态：指导下游渲染器与制造文件 (GDS/Gerber)
      bend_style: mitered_45          # 切角工艺：拐弯处必须执行 45° 倒角以减小寄生电容和信号反射

```

**种类 3：未规划的飞线 (`multipoint_net` / `trace`)**
由网表自然生成的普通低速/控制信号线，等待 P&R 算法去自动布线。

```yaml
  logical_bus_$34N821620:
    type: multipoint_net
    net: "$34N821620"
    routing_class: unrouted_bus
    connections: ["C731538.PIN_2", "C731660.PIN_2", "U731022.PIN_4"]

```

---

## 四、 开发对接指南 (For Downstream Developers)

下游引擎（布局布线器、电磁仿真软件、3D渲染器）开发者，请严格遵循以下处理管线（Pipeline）：

1. **画布与边界建立**：读取 `global_constraints.board_outline` 绘制基础面板。读取 `keepout_zones` 绘制禁止任何布线和器件进入的隔离区。
2. **放置“绝对钉子”**：遍历 `components`，将所有不含 `parametric_uv` 且 `is_floating: false` 的器件（如 `U731032` 芯片），依据其 `footprint_ref` 的尺寸数据，以绝对坐标（Absolute X/Y）锁定放置在画布上。
3. **铺设“射频铁轨”**：遍历 `edges`、`nodes` 和 `terminals`，优先将所有 `type: microstrip` 的物理骨架（结合 `constraint` 的线宽/长度与 `geometry.bend_style`）精确绘制在板框内。
4. **挂载“参数化车厢”**：遍历 `components`，提取含有 `parametric_uv` 属性的 RLC 周边器件，根据其 `reference_net` 定位到上一步铺设的射频微带线上。算法应计算出符合电气特性的 `offset_u` 和 `offset_v`，将其平滑吸附至微带线上。
5. **处理剩余“蜘蛛网”**：最后处理所有的普通 `trace` 和 `logical_bus`。调用推挤算法（Push & Shove Router）或 A* 算法，连通剩余未规划的逻辑管脚，同时必须避开第 1 步定义的 `keepout_zones` 和第 3 步铺设的 `rf_constrained` 微带线。
