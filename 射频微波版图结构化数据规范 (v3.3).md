
---

# 射频微波版图结构化数据规范 (RF Layout Data Specification v3.3)

## 一、 核心架构思想 (Architectural Philosophy)

本数据格式专为现代射频微波 EDA 工具设计，摒弃了传统网表（如 IPC-D-356 或纯连线表）的扁平化缺陷，采用以下三大核心架构思想：

1. **逻辑拓扑与物理实现的彻底解耦 (Separation of Logic and Physics)**
* **逻辑层**：由 `.tel` 网表驱动，保证所有的管脚连接关系（`pin_nets`）100% 完整，确保 LVS（版图与原理图一致性）检查永不出错。
* **物理层**：由 `edges`（微带线段）、`nodes`（物理节点）、`global_constraints`（全局约束）驱动，决定高频信号的精确几何形状与拓扑分叉。


2. **意图驱动与动态吸附 (Intent-Driven & Parametric Adhesion)**
* 彻底抛弃了全盘绝对坐标（X/Y）的死板布局。对于周边匹配网络（RLC 器件），引入基于局部微带线的 `parametric_uv`（UV 吸附）模型。器件不再是“放在板子绝对坐标上”，而是“挂在微带线骨架上”，将具体的推挤决策权下放给后端的自动布线算法。


3. **分层分级的大一统字典 (Unified Hierarchical Dictionary)**
* 文件聚合了项目元数据、全局规则、库资产、节点实体和连线拓扑，任何下游单一模块的读取都不会产生上下文缺失。



---

## 二、 全局数据字典层级 (Data Schema Overview)

整个 YAML 文件由 **7 个一级节点（Root Keys）** 构成，引擎必须按以下逻辑顺序读取：

```yaml
---
metadata:            # 1. 档案头信息（记录项目名、生成器版本、时间戳）
global_constraints:  # 2. 全局物理约束（画出板框和禁布区，加载默认线宽及板材）
footprints:          # 3. 封装资产库（将所有用到的焊盘尺寸存入内存缓存）
components:          # 4. 元器件实例（将器件放置在板框内或挂载到网络上）
terminals:           # 5. 物理骨架的外部端口（射频链路进出器件的锚点）
nodes:               # 6. 物理骨架的分支节点（阻抗阶跃、分路、合路、T型拐角）
edges:               # 7. 走线与拓扑（连接节点和端口，渲染微带线与普通飞线）

```

---

## 三、 详细字段说明与解析

### 1. `metadata` (元数据层)

**用途**：用于版本控制、项目管理和时间追溯。

```yaml
metadata:
  project_name: PA_Module_V1
  version: 1.0.0
  generator: EDA Auto-Layout Parser v3.3
  timestamp: 2026-05-12 12:00:00

```

### 2. `global_constraints` (全局边界与约束)

**用途**：划定 P&R 算法的物理活动范围，并提供电气默认值。这是布线器读取的**第一份重要数据**。

```yaml
global_constraints:
  routing:
    default_trace_width: 0.254  # 常规逻辑线默认线宽 (mm)
    rf_trace_width: 0.5         # 默认射频走线线宽
  stackup:
    layer_count: 4              # 层叠数量
    material: RO4350B           # 板材介电参数，用于电磁仿真及阻抗推算
  board_outline:                # 🌟 物理边界：算法推挤的绝对极限
    type: bounding_box
    origin: { x: 0.0, y: 0.0 }
    width: 40.0
    height: 185.0
  keepout_zones:                # 🌟 禁布区：打孔、螺丝定位孔，禁止任何走线穿过
  - id: ko_screw_hole_1
    type: circle
    center: { x: 10.0, y: 10.0 }
    radius: 3.5

```

### 3. `footprints` (封装资产库)

**用途**：局部极坐标系下的物理外形定义。将封装定义集中化，避免在 `components` 中产生海量冗余数据。

* **规则**：所有 `local_x` 和 `local_y` 必须以器件的**几何中心 (0,0)** 为基准。
* **规则**：封装名与管脚名必须使用双引号。

```yaml
footprints:
  "SC0603-TE":
    description: "3-Pin RF Transistor Package"
    dimensions: { width: 1.00, length: 1.85, height: 0.9 }
    pins:
      "PIN_1":
        local_x: -0.70
        local_y: 0
        local_orientation: 180  # 默认出线方向（角度）
        pad_geometry:
          shape: "rect"
          width: 0.91
          length: 0.97

```

### 4. `components` (元器件拓扑实例)

**用途**：承载网表的逻辑连接关系（`pin_nets`）以及物理放置意图（`placement`）。
根据 `placement.type` 区分两种布局形态：

#### 形态 A：绝对坐标锚定 (Fixed/Absolute Placement)

适用于主干芯片、接插件等预先在 Floorplan 阶段定死的器件。

```yaml
components:
  U731032:
    footprint_ref: "RF4-SUM-HC12"
    placement:
      x: 29.6           # 全局绝对坐标
      y: 78.5
      rotation: 0
      is_floating: false # 锁死标志：P&R 算法绝对不能推挤它
    pin_nets:
      "PIN_1": "$36N850545"

```

#### 形态 B：UV 参数化吸附 (Parametric UV Adhesion) 🌟 [意图驱动核心]

适用于周边射频匹配网络（旁路电容、串联电感等）。它们“寄生”在逻辑网络上，交由布线算法在微带线上动态滑动寻找最佳位置。

```yaml
  C731312:
    footprint_ref: "SC0402-PA"
    placement:
      type: parametric_uv           # 开启 UV 拓扑吸附
      anchor_pin: "PIN_2"           # 用该器件的 PIN_2 作为接触点
      reference_net: "$36N998423"   # 🌟 挂载目标：它要依附在这个网络上
      origin:
        offset_u: null              # 沿线长度 (交由算法推算最优电气位置)
        offset_v: auto              # 横向偏移 (交由算法决定摆在走线左侧还是右侧)
    pin_nets:
      "PIN_2": "$36N998423"

```

---

### 5. `terminals`, `nodes`, `edges` (射频物理骨架三要素) 🌟 [微波拓扑核心]

在射频微波设计中，一条网络（Net）往往不是单一宽度的线，而是由多段不同宽度的微带线（Edges）、阻抗阶跃点/分路器（Nodes）、以及器件接触点（Terminals）拼接而成的复杂拓扑。

#### 5.1 `terminals` (物理端口/锚点)

**用途**：定义微带线骨架与外部世界（器件焊盘）的交汇点。

```yaml
terminals:
  U731031_pin1_start:
    type: component_pad           # 端口类型：器件焊盘
    associated_component: U731031 # 绑定的目标器件
    pin: "PIN_1"                  # 绑定的目标管脚
    net: "$36N996675"

```

#### 5.2 `nodes` (物理节点/连接器)

**用途**：微带线段与线段之间的交汇点。在 EM 电磁仿真中，这些节点代表着极其重要的“寄生参数突变区”。

```yaml
nodes:
  # 场景 1：阻抗阶跃点 (Impedance Step) - 两段不同宽度的微带线直接相连
  node_step_1:
    type: impedance_step
    net: "$36N996675"
    description: "50欧姆到35欧姆的阻抗突变点，可能需要倒角或渐变处理"

  # 场景 2：一分二功率分配节点 (Splitter / T-Junction)
  node_splitter_wilkinson:
    type: t_junction
    net: "$36N996675"
    description: "Wilkinson 功分器的中心分叉点"
    
  # 场景 3：无缝拼接节点 (Universal Node) - 相同线宽的拐弯或延长连接
  node_corner_1:
    type: universal_node
    net: "$36N996675"

```

#### 5.3 `edges` (连线与微带线段)

**用途**：定义两点（Node/Terminal）之间的物理轨迹。

**种类 A：预定义的物理微带线 (`microstrip`)**
它是“意图驱动”的最高体现，支持后端 EM（电磁仿真）和精确的阻抗控制。

```yaml
edges:
  U731031_pin1_seg1:
    type: microstrip
    net: "$36N996675"
    routing_class: rf_constrained   # 算法路权：绝对优先，其他普通走线需避让
    connections:
    - U731031_pin1_start            # 起点：Terminal (芯片管脚)
    - node_step_1                   # 终点：Node (阻抗阶跃点)
    constraint:                     # 物理约束：保障射频信号完整性 (SI)
      width: 0.94                     # 强制线宽 (如对应 50Ω)
      target_length: 1.21             # 相位约束：强制走线物理长度
    geometry:                       # 几何形态：指导制造文件 (GDS/Gerber)
      bend_style: mitered_45          # 拐弯工艺：必须执行 45° 倒角以减小信号反射

```

**种类 B：纯逻辑户口本 (`logical_net`)**
当微带线骨架（上述 `terminals`+`nodes`+`microstrip`）已经定义好后，系统必须保留一份纯逻辑网络，以记录挂载在该网络上的所有 RLC 器件，确保 LVS 不报错。

```yaml
  logical_net_$36N996675:
    type: logical_net
    net: "$36N996675"
    routing_class: externally_constrained # 标记：已有外部物理骨架
    connections: ["U731031.PIN_1", "R731130.PIN_1", "C731312.PIN_2"] # 逻辑校验基准

```

**种类 C：未规划的飞线 (`trace` / `multipoint_net`)**
由网表自然生成的普通低速/控制信号线，等待 P&R 算法去自动布线。

```yaml
  trace_$34N821620_1:
    type: multipoint_net
    net: "$34N821620"
    routing_class: unrouted_bus
    connections: ["C731538.PIN_2", "U731022.PIN_4"]

```

---

## 四、 开发对接指南 (For Downstream Developers)

下游引擎（布局布线器、电磁仿真软件、3D渲染器）开发者，请严格遵循以下处理管线（Pipeline）：

1. **画布与边界建立**：
* 读取 `global_constraints.board_outline` 绘制基础面板。
* 读取 `keepout_zones` 绘制禁止任何布线和器件进入的隔离区。


2. **放置“绝对钉子” (Fixed Placements)**：
* 遍历 `components`，将不含 `parametric_uv` 且 `is_floating: false` 的主干器件（如芯片），依据 `footprint_ref` 尺寸，以绝对坐标锁定放置在画布上。


3. **拼装“射频微带线骨架” (RF Backbone Construction)**：
* 提取所有 `terminals` 作为骨架的受力点（锚定在主干芯片的焊盘上）。
* 提取所有 `nodes` 作为骨架的分叉点、阶跃点。
* 遍历 `edges` 中 `type: microstrip` 的数据，依据 `constraint` (线宽/长度) 和 `connections` (连接关系)，将 `terminals` 与 `nodes` 像乐高一样拼接起来，绘制出具备精确阻抗与相位的微带铜皮。


4. **挂载“参数化车厢” (UV Component Adhesion)**：
* 遍历 `components` 中含有 `parametric_uv` 属性的匹配网络器件 (RLC)。
* 根据其 `reference_net` 定位到上一步铺设的微波骨架上。算法通过计算最优空间位置，得出 `offset_u` 和 `offset_v`，将其焊盘平滑吸附至微带线上。


5. **处理剩余“蜘蛛网” (Auto-Routing)**：
* 最后处理所有普通 `trace` 和 `multipoint_net`。
* 调用推挤算法 (Push & Shove Router) 连通剩余逻辑管脚，在此过程中，**必须避开**第 1 步定义的 `keepout_zones` 以及第 3 步铺设完毕的 `rf_constrained` 微带骨架区。
